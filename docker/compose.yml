services:
  server:
    build:
      context: ../server
    environment:
      NODE_ENV: ${NODE_ENV}
      PORT: ${PORT}
      ASSETS_BASE_URL: ${ASSETS_BASE_URL}
      DATABASE_URL: ${DATABASE_URL}
      SESSION_SECRET: ${SESSION_SECRET}

  client:
    build:
      context: ../client
